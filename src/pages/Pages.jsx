import { useEffect, useState } from 'react'
import { ArrowRight, BarChart3, Check, ChevronDown, Code2, Fingerprint, Mail, MapPin, Phone, ShieldCheck, WalletCards, Zap } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import site from '../config/site'
import { allApis, allSoftware, findCatalogItem, industryItems, serviceItems } from '../data/catalog'
import { ApiConsole, InfrastructureGraph, NetworkVisualization, ProductMockup, ScrollReveal, SecurityDashboard } from '../components/Visuals'
import { submitContactForm } from '../utils/contact'

function Seo({ title, description }) {
  useEffect(() => { document.title = `${title} | NTSFINCO`; const meta = document.querySelector('meta[name="description"]') || document.head.appendChild(Object.assign(document.createElement('meta'), { name: 'description' })); meta.setAttribute('content', description) }, [title, description])
  return null
}

function SectionIntro({ eyebrow, title, text, align = '' }) { return <div className={`section-intro ${align}`}><span className="eyebrow">{eyebrow}</span><h2>{title}</h2>{text && <p>{text}</p>}</div> }
function Button({ to = '/contact', children, secondary = false }) { return <Link className={`button ${secondary ? 'button-secondary' : ''}`} to={to}>{children}<ArrowRight size={16} /></Link> }
function Metric({ value, label }) { return <div className="metric"><strong>{value}</strong><span>{label}</span></div> }

function HomePage() {
  const [faq, setFaq] = useState(0)
  const faqs = [['What does NTSFINCO provide?', 'NTSFINCO brings software, APIs and product engineering together for teams building financial and commerce workflows.'], ['Can we integrate only one capability?', 'Yes. The platform is modular, so a team can start with one API or operating workflow and grow from there.'], ['Does the site represent a live backend?', 'The current experience is a product interface and integration concept. Production API connectivity depends on your backend configuration.']]
  return <><Seo title="Fintech Infrastructure Platform" description="NTSFINCO connects fintech software, APIs and product engineering for dependable financial workflows." /><section className="hero"><div className="hero-grid"><div className="hero-copy"><span className="eyebrow"><i className="status-dot" /> FINTECH INFRASTRUCTURE / 2026</span><h1>Make complex money movement feel <em>simple.</em></h1><p className="hero-lede">NTSFINCO gives product teams a clear operating layer for payments, verification, banking and the everyday financial services people depend on.</p><div className="hero-actions"><Button to="/software">Explore platform</Button><Button to="/contact" secondary>Talk to sales</Button></div><div className="hero-trust"><span><ShieldCheck size={16} /> Built with control in mind</span><span><Zap size={16} /> Integration-first</span></div></div><div className="hero-art"><InfrastructureGraph /></div></div><div className="hero-signal"><span>01</span><span>ONE CONTROL PLANE</span><div /><span>SCROLL TO EXPLORE</span></div></section><section className="metrics-band"><div className="container metrics"><Metric value="Modular" label="API and product building blocks" /><Metric value="Connected" label="Across financial operations" /><Metric value="Observable" label="From request to settlement" /><Metric value="Human" label="Support for complex workflows" /></div></section><section className="section platform-section"><div className="container split-grid"><ScrollReveal><SectionIntro eyebrow="THE PLATFORM" title="A calmer way to run financial infrastructure." text="The work behind a financial product is rarely one transaction. It is identity, routing, reconciliation, risk and the people who need to see what happened." /><div className="feature-list"><div><span>01</span><p><b>Connect the edges</b><br />Give every channel a consistent route into your operations.</p></div><div><span>02</span><p><b>See the whole path</b><br />Move from request to outcome without losing context.</p></div><div><span>03</span><p><b>Build for change</b><br />Keep the product layer flexible as your network evolves.</p></div></div></ScrollReveal><ScrollReveal className="visual-column"><ProductMockup /></ScrollReveal></div></section><section className="section dark-section"><div className="container"><SectionIntro eyebrow="CAPABILITIES" title="The pieces your financial product needs." text="Start with a focused workflow or design a connected operating environment across software, APIs and services." /><div className="capability-grid">{[['Banking rails', 'AEPS, DMT and assisted access workflows.', WalletCards], ['Recharge network', 'Mobile, biller and recurring payment journeys.', Zap], ['Verification', 'Identity and account checks designed for review.', Fingerprint], ['Payout operations', 'Move from instruction to settlement visibility.', CircleDollarIcon], ['Developer layer', 'Documented integration concepts and clear states.', Code2], ['Risk controls', 'Permission, audit and monitoring as part of the flow.', ShieldCheck]].map(([title, text, Icon]) => <Link className="capability-card" to="/software" key={title}><span className="card-icon"><Icon size={19} /></span><h3>{title}</h3><p>{text}</p><ArrowRight size={16} /></Link>)}</div></div></section><section className="section"><div className="container split-grid reverse-mobile"><ScrollReveal className="visual-column"><ApiConsole /></ScrollReveal><ScrollReveal><SectionIntro eyebrow="FOR DEVELOPERS" title="A clear path from request to response." text="Make integrations easier to reason about with consistent endpoints, understandable states and workflows your team can observe." /><div className="inline-link"><Link to="/api/recharge">Explore API solutions <ArrowRight size={16} /></Link></div></ScrollReveal></div></section><section className="section security-section"><div className="container split-grid"><ScrollReveal><SectionIntro eyebrow="CONTROL BY DESIGN" title="Trust is built into the workflow." text="Design your financial operations around authentication, auditability, data protection and risk visibility from the beginning." /><Button to="/about" secondary>See how we work</Button></ScrollReveal><ScrollReveal className="visual-column"><SecurityDashboard /></ScrollReveal></div></section><section className="section"><div className="container"><SectionIntro eyebrow="BUILT FOR MANY TEAMS" title="One foundation, different operating realities." /><div className="industry-strip">{industryItems.slice(0, 6).map((item) => <Link to={item.route} key={item.slug}><span>{item.title}</span><ArrowUpRightIcon /></Link>)}</div></div></section><section className="section faq-section"><div className="container split-grid"><SectionIntro eyebrow="QUESTIONS, ANSWERED" title="Start with the right conversation." text="A platform should clarify decisions, not add another layer of uncertainty." /><div className="faq-list">{faqs.map(([question, answer], index) => <div className={`faq-item ${faq === index ? 'open' : ''}`} key={question}><button onClick={() => setFaq(faq === index ? -1 : index)}><span>{question}</span><ChevronDown size={17} /></button>{faq === index && <p>{answer}</p>}</div>)}</div></div></section><CtaBand /></>
}

function CircleDollarIcon(props) { return <BarChart3 {...props} /> }
function ArrowUpRightIcon() { return <ArrowRight size={15} /> }

function AboutPage() { return <><Seo title="About" description="Learn how NTSFINCO approaches fintech infrastructure and connected financial workflows." /><PageHero eyebrow="ABOUT NTSFINCO" title="Building the operating layer behind digital finance." text="Rakle Service Private Limited brings product thinking and financial workflow discipline together for businesses that need their technology to keep up." visual={<NetworkVisualization />} /><section className="section"><div className="container story-grid"><div><SectionIntro eyebrow="WHO WE ARE" title="Useful technology is specific technology." /></div><div className="story-copy"><p>We focus on the practical details that make financial products dependable: the handoff between systems, the status a team sees when something changes, and the controls that help people make better decisions.</p><p>Our work spans software platforms, financial APIs and digital product engineering. The common thread is a preference for clear systems over noisy complexity.</p></div></div></section><section className="section dark-section"><div className="container values-grid">{[['Mission', 'Make financial infrastructure easier to connect, operate and improve.'], ['Vision', 'A world where more businesses can build trusted financial experiences without rebuilding every rail.'], ['Working style', 'Small feedback loops, visible trade-offs and interfaces that respect the operator.']].map(([title, text]) => <div className="value-block" key={title}><span className="eyebrow">{title}</span><h3>{text}</h3></div>)}</div></section><section className="section"><div className="container split-grid"><div><SectionIntro eyebrow="HOW WE THINK" title="Infrastructure is also a people problem." text="Good systems give teams context, not just speed. We design for the operator who needs to understand the transaction, the developer who needs to integrate it, and the customer who simply needs it to work." /></div><ProductMockup kind="security" /></div></section><CtaBand /></> }

function PageHero({ eyebrow, title, text, visual }) { return <section className="page-hero"><div className="container page-hero-grid"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{text}</p></div>{visual && <div className="page-hero-visual">{visual}</div>}</div></section> }

function CatalogPage({ type }) { const { slug } = useParams(); const item = findCatalogItem(type, slug); if (!item) return <NotFound />; const isApi = type === 'api'; const isIndustry = type === 'industries'; const related = (type === 'software' ? allSoftware : type === 'api' ? allApis : type === 'services' ? serviceItems : industryItems).filter((entry) => entry.slug !== item.slug).slice(0, 3); return <><Seo title={item.title} description={item.description} /><PageHero eyebrow={`${item.category.toUpperCase()} / ${item.slug.toUpperCase()}`} title={item.title} text={item.description} visual={isApi ? <ApiConsole /> : isIndustry ? <NetworkVisualization /> : <ProductMockup kind={type === 'services' ? 'api' : 'platform'} />} /><section className="section"><div className="container split-grid"><div><SectionIntro eyebrow="THE OPPORTUNITY" title={isApi ? 'A focused integration path for your product.' : isIndustry ? 'Design the workflow around the people using it.' : 'Turn operational complexity into a product advantage.'} text={`Build a more legible ${item.title.toLowerCase()} experience with a foundation that keeps the important details visible.`} /><ul className="check-list"><li><Check size={16} /> Clear states from initiation to outcome</li><li><Check size={16} /> Flexible roles for teams and partners</li><li><Check size={16} /> A foundation ready for measured iteration</li></ul></div><InfrastructureGraph /></div></section><section className="section dark-section"><div className="container"><SectionIntro eyebrow="WORKFLOW" title="A useful system makes the next step obvious." text="Map the handoffs, permissions and outcomes before adding surface complexity." /><div className="workflow-grid">{['Capture the request', 'Validate the context', 'Process the instruction', 'Surface the outcome'].map((step, index) => <div key={step}><span>0{index + 1}</span><h3>{step}</h3><p>{['Start with the intent and the information your user already has.', 'Apply the controls and checks the workflow requires.', 'Route the work through the right service or operating queue.', 'Give people a clear status, record and next action.'][index]}</p></div>)}</div></div></section><section className="section"><div className="container"><SectionIntro eyebrow="WHAT TO EXPLORE NEXT" title="Keep building from the same foundation." /><div className="related-grid">{related.map((entry) => <Link className="related-card" to={entry.route} key={entry.slug}><span className="eyebrow">{entry.category}</span><h3>{entry.title}</h3><p>{entry.description}</p><ArrowRight size={17} /></Link>)}</div></div></section><CtaBand /></> }

function SoftwareIndex() { return <><Seo title="Software" description="Explore NTSFINCO software platforms for recharge, banking, verification and operations." /><PageHero eyebrow="SOFTWARE SYSTEMS" title="Software that gives financial networks room to move." text="Choose an operating surface for the work your teams already do, then connect it to the rest of your stack." visual={<ProductMockup />} /><CatalogIndex items={allSoftware} /></> }
function ApiIndex() { return <><Seo title="API Solutions" description="Explore NTSFINCO APIs for recharge, fintech, travel and verification workflows." /><PageHero eyebrow="API SOLUTIONS" title="Financial capabilities, ready to connect." text="Compose the services your product needs with a clear integration model and observable workflow states." visual={<ApiConsole />} /><CatalogIndex items={allApis} /></> }
function CatalogIndex({ items }) { return <section className="section"><div className="container"><div className="catalog-grid">{items.map((item) => <Link className="catalog-card" to={item.route} key={item.slug}><span className="catalog-number">{String(items.indexOf(item) + 1).padStart(2, '0')}</span><div><span className="eyebrow">{item.category}</span><h3>{item.title}</h3><p>{item.description}</p></div><ArrowRight size={17} /></Link>)}</div></div></section> }

function CareerPage() { const roles = [['Product Engineer', 'Product systems', 'Build interfaces that make complex workflows understandable.'], ['Integration Engineer', 'Platform', 'Shape dependable connections between products and financial services.'], ['Product Designer', 'Design', 'Turn operational detail into calm, useful experiences.']]; return <><Seo title="Career" description="Explore the kind of product, platform and design work happening at NTSFINCO." /><PageHero eyebrow="CAREERS" title="Work on systems people rely on." text="We are interested in thoughtful builders who care about the details between an idea and a dependable product." visual={<NetworkVisualization />} /><section className="section"><div className="container split-grid"><SectionIntro eyebrow="WHY NTSFINCO" title="Make technical work feel useful." text="Our work sits close to real business operations. That means learning quickly, communicating clearly and making decisions you can explain." /><div className="values-stack">{['Visible decisions', 'Room to learn', 'Respect for the operator'].map((value) => <div key={value}><Check size={17} /><span>{value}</span></div>)}</div></div></section><section className="section dark-section"><div className="container"><SectionIntro eyebrow="OPEN ROLES" title="Roles we are shaping around the work." text="These role outlines are a starting point for conversations, not a promise of an open position." /><div className="role-grid">{roles.map(([title, team, text]) => <div className="role-card" key={title}><span className="eyebrow">{team}</span><h3>{title}</h3><p>{text}</p><Link to="/contact">Start a conversation <ArrowRight size={15} /></Link></div>)}</div></div></section><CtaBand /></> }

function ContactPage() { const [form, setForm] = useState({ name: '', email: '', phone: '', company: '', requirement: '', message: '' }); const [status, setStatus] = useState('idle'); const update = (event) => setForm({ ...form, [event.target.name]: event.target.value }); const submit = async (event) => { event.preventDefault(); setStatus('loading'); try { await submitContactForm(form); setStatus('success') } catch { setStatus('error') } }; return <><Seo title="Contact" description="Talk with NTSFINCO about fintech infrastructure, APIs, software and product engineering." /><PageHero eyebrow="CONTACT NTSFINCO" title="Let's build the next financial experience." text="Tell us what you are trying to connect, improve or launch. We will use the details to make the first conversation useful." visual={<NetworkVisualization />} /><section className="section contact-section"><div className="container contact-grid"><div className="contact-details"><SectionIntro eyebrow="START A CONVERSATION" title="Bring us the messy version." text="A short outline is enough. We can work through the architecture together." />{site.email && <a href={`mailto:${site.email}`}><Mail size={17} />{site.email}</a>}{site.phone && <a href={`tel:${site.phone}`}><Phone size={17} />{site.phone}</a>}{site.address && <span><MapPin size={17} />{site.address}</span>}</div><form className="contact-form" onSubmit={submit}><div className="form-row"><label>Name<input required name="name" value={form.name} onChange={update} /></label><label>Email<input required type="email" name="email" value={form.email} onChange={update} /></label></div><div className="form-row"><label>Phone<input name="phone" value={form.phone} onChange={update} /></label><label>Company<input name="company" value={form.company} onChange={update} /></label></div><label>What are you exploring?<select name="requirement" value={form.requirement} onChange={update}><option value="">Select a focus</option><option>Software platform</option><option>API integration</option><option>Product engineering</option><option>Something else</option></select></label><label>Message<textarea required name="message" rows="5" value={form.message} onChange={update} /></label><button className="button" type="submit" disabled={status === 'loading'}>{status === 'loading' ? 'Preparing...' : 'Send enquiry'} <ArrowRight size={16} /></button>{status === 'success' && <p className="form-success"><Check size={16} /> Your enquiry is ready for a backend connection. No message was sent from this demo.</p>}{status === 'error' && <p className="form-error">Please complete the required fields and try again.</p>}</form></div></section></> }

function CtaBand() { return <section className="cta-band"><div className="container cta-inner"><div><span className="eyebrow">BUILD WITH CLARITY</span><h2>Give your next financial workflow a stronger foundation.</h2></div><Button>Talk to our team</Button></div></section> }
function NotFound() { return <><Seo title="Transaction Not Found" description="The requested NTSFINCO route could not be found." /><section className="not-found"><span className="eyebrow">404 / TRANSACTION NOT FOUND</span><h1>This route did not settle.</h1><p>The page may have moved, but the platform is still here.</p><div className="hero-actions"><Button to="/">Return home</Button><Button to="/software" secondary>Explore platform</Button></div></section></> }

export { AboutPage, ApiIndex, CareerPage, CatalogPage, ContactPage, HomePage, IndustryIndex, NotFound, ServiceIndex, SoftwareIndex }

function IndustryIndex() { return <><Seo title="Industries" description="Explore industry workflows supported by NTSFINCO infrastructure." /><PageHero eyebrow="INDUSTRIES" title="Financial infrastructure shaped around context." text="Different industries carry different constraints. Start with the operating reality, then build the right connected workflow." visual={<NetworkVisualization />} /><CatalogIndex items={industryItems} /></> }
function ServiceIndex() { return <><Seo title="Services" description="Explore NTSFINCO product engineering, hosting, design and growth services." /><PageHero eyebrow="SERVICES" title="Product capability where your roadmap needs it." text="Bring a focused team into the part of the product that needs more clarity, speed or operational depth." visual={<ProductMockup kind="api" />} /><CatalogIndex items={serviceItems} /></> }
